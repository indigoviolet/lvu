{
  "name": "@lvu/paseo-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": "26.8.1" },
  "bin": { "lvu-paseo-bridge": "dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm test"
  },
  "dependencies": {
    "@getpaseo/client": "0.7.2",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "26.0.0",
    "typescript": "6.0.2",
    "vitest": "4.1.6"
  }
}
