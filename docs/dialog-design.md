# Dialog presentation (superseded)

This document was merged into [`dialog-system.md`](dialog-system.md) on
2026-09-08. The layout guidance had been superseded since that document was
written; the three control rules that lived only here (the meaning of each
visual treatment, the keyboard-binding policy, and the colour-test rule) are
now `dialog-system.md` §8.15.

The rendered-evidence audit that occupied §1–§12 here, taken on main
`1d0c44f`, described dialogs that no longer exist and is in this file's
history at `14ad91c`, with its raw captures in `dialog-audit-captures.md` at
the same revision. Code comments that cite `dialog-design.md §3` mean the
overflowing-input rule, which is `dialog-system.md` §8.1 and §13.
